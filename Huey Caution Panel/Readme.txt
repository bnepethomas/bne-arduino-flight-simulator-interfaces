A project which uses a Raspberry pi and Max 7219 to display warning panel

The lua files are added to the SDA sciprts, as SDA is used to drive the primary instruments monitor
Oriingally alerts were being sent to a SOIC card which drove other leds in the Jet Ranger
Will need to add entires in config file to Point to IP address and then just send 
all attributes and select on the file which is of interest.

Attributes are received as A=V:A1=V1:A2=V2