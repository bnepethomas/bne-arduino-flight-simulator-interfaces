# Both FlightSim and Aircraft type are important
# as they determine which methods of communication is used
# Currently this is largely due to XPlane using packed strings
# versus the free text used by DCS
learning = False
AircraftType = 'A10C'
FlightSim = 'DCS'
